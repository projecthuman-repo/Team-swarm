{
  "name": "swarm-outbox-relay",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "description": "Transactional-outbox relay: the only publisher into NATS JetStream.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "tsx relay.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "nats": "^2.29.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}

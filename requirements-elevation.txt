# v4.1 "Elevation" additions (T0.3) — opt-in, matching the repo's
# granular-requirements convention. Run `make gate` after installing:
# the fail-closed license gate must stay green (no copyleft in the tree).
#
# headroom-ai: Apache-2.0 (chopratejas/headroom — the compression layer,
#   NOT gglucass/headroom-desktop). Kompress-v2-base model + bundled RTK
#   binary licenses are verified at pin time by the syft gate (fail-closed
#   either way).
headroom-ai==0.31.0
# litellm: MIT core only — the enterprise/ directory is not used.
litellm>=1.60,<2
# capacity probe (T4.1)
nvidia-ml-py>=12.560
psutil>=6.0
# v4.2 T7.7 — Claude Agent SDK escalation engine, API-fallback lane ONLY
# (Claude-model-only, Anthropic egress; never in the sealed container).
# Optional-guarded in agent/escalation_claude.py.
claude-agent-sdk>=0.2

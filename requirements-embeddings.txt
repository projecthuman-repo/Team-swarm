# Optional: local embeddings for writable vector memory (agent/lessons.py).
# Default model: all-MiniLM-L6-v2 -> vector(384) (db/schema.sql).
# Quality upgrade (runbook §18): EMBEDDING_MODEL=BAAI/bge-m3,
# EMBEDDING_DIM=1024 + db/migrations/001_bge_m3_embeddings.sql.
# Apache-2.0 / MIT.
sentence-transformers>=3.0
